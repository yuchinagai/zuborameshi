<div class="col-lg-12 col-md-12 col-sm-12 mb-3 text-center">
<h2><%= t '.title' %></h2>
  <div class="row">
    <% @howtos.each do |howto| %>
      <div class="col-md-4">
        <%= show_howto_image howto %>
        <p>
          <%= link_to howto.title, howto_path(howto) %>
        </p>
      </div>
    <% end %>
  </div>
</div>