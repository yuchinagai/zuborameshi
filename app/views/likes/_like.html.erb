<div class="like-link" id="like-link-<%= @recipe.id %>">
  <% if current_user.likes.find_by(recipe_id: @recipe.id) %>
    <%= link_to like_path(recipe.id), method: :post, remote: true do %>
        <div class = "iine_button">いいね <%= @recipe.likes.count %></div>
    <% end %>
  <% else %>
    <%= link_to like_path(recipe.id), method: :post, remote: true do %>
        <div class = "iine_button">いいね<%= @recipe.likes.count %></div>
    <% end %>
  <% end %>
</div>