<div>
  <div>
    <% if review.user.avatar.attached? %>
      <%= image_tag review.user.avatar.variant(resize: "60x60"), class: "img-thumbnail" %>
    <% else %>
      <%= image_tag "img_warning.png", class: "img-thumbnail", width: 60 %>
    <% end %>
    <span><%= review.user.user_name %></span>
  </div>
  <div>
    <div>
      <%= review.evaluation %>
      <b><%= link_to review.body, recipe_review_path(review.recipe, review) %></b>
    </div>
    <small><%= review.created_at.strftime("%Y年%m月%d日") %></small>
  </div>
  <%= simple_format review.body %>
</div>